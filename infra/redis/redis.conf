# Redis configuration for revWave

# Persistence
appendonly yes
appendfsync everysec

# Memory management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice

# Security (for production, set requirepass)
# requirepass your-redis-password
